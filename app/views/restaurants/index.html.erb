<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
    </li>
  <% end %>
</ul>
<button type="button" class="btn btn-default"><%= link_to("Create New Restaurant", new_restaurant_path) %></button>
